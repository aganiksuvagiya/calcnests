/**
 * Single on/off switch for every ad slot on the site. Flip to `false` to
 * pull all ad placeholders — and the layout space they reserve — sitewide
 * without touching individual pages. This is where a real ad network's
 * "master enable" flag would live once one is wired in.
 */
export const ADS_ENABLED = false;
